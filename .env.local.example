# Copy this file to .env.local (already gitignored) and fill in the real Secret Key
# yourself in a text editor. Never paste the real value into chat or commit it — see
# docs/PLAYFAB_SETUP.md's "Handling the Secret Key".
#
#   cp .env.local.example .env.local
#   nano .env.local        # or any editor — fill in PLAYFAB_SECRET_KEY below
#   source .env.local && dotnet run --project src/LTW.MatchServer

export PLAYFAB_TITLE_ID="FBC34"
export PLAYFAB_SECRET_KEY="paste-the-real-secret-key-here"
