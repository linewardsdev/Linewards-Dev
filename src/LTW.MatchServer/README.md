# LTW Match Server

The authoritative .NET match server is intentionally deferred until the offline MVP proves the local three-player loop.

When online work begins, this boundary will host a headless service that references `LTW.Simulation`. MVP-00 does not create a server runtime, transport, database, authentication system, or cloud dependency.
